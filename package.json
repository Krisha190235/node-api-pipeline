{
  "name": "node-api",
  "version": "1.0.0",
  "main": "src/app.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand --coverage",
    "lint": "eslint .",
    "build": "echo \"(optional) ts compile or bundle step\" && exit 0",
    "audit": "npm audit --audit-level=high || true"
  },
  "dependencies": {
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "nodemon": "^3.1.0"
  }
}